"""身份与授权上下文。

设计依据：
- 04 号规格 §1 —— 数据层级 Tenant → User → Learning Project。
- 06 号规格 §2 —— L2 边界层负责身份与租户上下文注入。
- 不变量 #1 —— 未通过身份、租户和项目校验的请求不得读取项目数据。

**本模块存在的意义**：消除「客户端自报身份」。此前 `tenant_id` 来自请求体，
等于任何调用方都能声称自己是别的租户。现在身份只能来自**服务端签名的会话令牌**，
请求体里根本没有这个字段可填。
"""
