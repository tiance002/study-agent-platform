# 学习规划助手设计任务

## 2026-09-21 开放注册实现进度

- [x] 契约基线：用户名 Unicode/Argon2、会话元数据与双开关冻结并写入规格
- [x] `0012` 迁移：凭据、会话来源核验、默认项目原子注册、登录函数、预算生命周期表
- [x] 后端：内存/PG 账号仓储、注册/登录/登出语义、计算池、限流容量和 no-store
- [x] 费用派发：真实 provider 派发前平台级原子预留，worker 按结果结算/释放，PG 状态持久化
- [x] 前端：邀请码、密码登录、开放注册三种入口及账号切换提示
- [x] 运维：发布顺序、告警处置、无邮箱账号生命周期与回滚手册
- [x] PostgreSQL 专项：本机实例恢复后完成 `0012` 升降级往返、调用角色权限和全量 PostgreSQL 门禁

### 2026-09-22 审查收口

- [x] 修正 `0012` 的预算函数授权、数据库 UTC 月份约束和降级路径
- [x] 补齐账户凭据 RLS、认证审计单写入、流式请求体上限和前端敏感字段清理
- [x] 将付费预留改为可由 `run_id` 重建的稳定标识，并修正 worker 终态提交顺序
- [x] 运行完整后端测试、完整 PostgreSQL 标记测试、Ruff、mypy、Python 编译和前端语法门禁
- [ ] 将 `0012` 和本轮代码发布到 ECS，先备份再迁移，并执行生产注册/登录/教学冒烟；本轮没有操作生产服务器

### 2026-09-22 首版生产验收阶段

- [x] ECS `0012` 发布、开放注册开关和默认登录界面上线
- [x] 用真实浏览器完成注册、登出、登录和 Cookie 会话恢复
- [x] 用普通用户完成建项目、建会话、发送消息、保存计划、登记资料
- [x] 重启 web/worker 后确认上述持久状态仍完整
- [ ] 在用户明确承担模型调用费用后，做一次真实教学 provider 联调并记录 usage/预算结算
- [ ] 通过首版安全、可访问性、备份恢复和回滚退出门后，标记普通用户首版可用

## 2026-09-22 本机 PostgreSQL 永久修复与发布前收口

- [x] 确认本机根因：没有 PostgreSQL 服务、没有监听 5432，`E:\pgsql` 数据目录存在但只有遗留 `postmaster.pid`
- [x] 注册 `study-plan-postgresql` Windows 服务，设置自动启动与 `NetworkService` 服务账号
- [x] 收紧 `E:\pgsql\data` 权限并验证服务重启后 `127.0.0.1:5432` 仍可用
- [x] 本机数据库迁移到 `0013`，增加无限平台月度预算语义（`NULL` 只取消金额拒绝，继续记账）
- [x] 修复应用启动自检仍写死 `0012` 的缺陷
- [x] 本机真实 PostgreSQL 全量门禁：`765 passed, 1 skipped`
- [ ] 将 `0013` 与 schema 期望版本发布到 ECS，并重新验证云端重启与真实 provider

当前阻塞：provider 配置已加载，但 PostgreSQL `platform_budget_config` 仍为
`paid_dispatch_enabled=false`、`monthly_cap_micro=0`。真实联调在派发前被
`PLATFORM_BUDGET_EXCEEDED` 拒绝，未产生模型费用；启用前需要用户明确平台月度
额度（整数微单位）和是否允许真实调用。

本阶段优先验证现有能力和生产闭环；向量检索、reranker、PDF/OCR 等增强项排在首版之后。

## 2026-09-21 开放注册修订方案缺口审查

目标：将用户提供的修订方案与当前仓库实现、迁移和生产发布约束逐项对照，识别仍缺失或需要进一步冻结的安全、数据、一致性、运维与验收要求；本轮只审查，不修改业务代码。

- [x] 读取修订方案与既有规划上下文
- [x] 核验当前分支、迁移头、认证/会话/预算/前端实现
- [x] 形成按严重性排序、带代码证据的缺口清单
- [x] 更新审查记录并交付结论

## 2026-09-20 持续交付任务

目标：复审并修复第 1、2 轮，随后逐轮交付到普通用户可使用的首版；每轮通过机械退出门后提交并推送 GitHub。

- [x] 第 1 轮复审收口：认证安全、可靠审计、代理限流、生产配置、PostgreSQL 退出门
- [x] 第 2 轮复审收口：全写端点幂等、崩溃恢复、乐观锁、持久化重启闭环
- [x] 第 3 轮：学习闭环 MVP（目标、诊断、计划、任务、提交、证据、掌握度）
- [x] 第 4 轮：资料摄取与可引用检索（durable 摄取、结构化切块、中文关键词检索、可核验引用）
- [x] 第 4 轮收口：独立审查 7 条（4×P1 + 3×P2）逐条复核成立并修复，迁移到 `0009`
- [x] 第 5 轮：持久化模型教学交互（durable teaching run、预算预留、引用校验、worker 故障恢复、SSE 回放；真实 provider 待凭据接入）
- [x] 第 6 轮：教学可靠性收口、请求快照、完整预算上界、引用交付、OpenAI Responses 离线适配器
- [x] 第 7 轮：React 普通用户首版界面与浏览器工作流（历史实现；第八轮复审发现的正确性问题见记录）
- [ ] 第 8 轮：学习工作台正确性收口（代码修复与门禁完成；R8-H/R8-I 的完整重启/中断 HTTP 场景仍未完成）
- [ ] 后续轮次：向量/语义检索与 reranker、真实 provider 生产联调、部署与发布门
- [ ] 首版验收：新用户无需终端即可完成核心学习闭环，数据可恢复，安全/可访问性/运维门通过

当前检查点：**第 1-8 轮代码修复和本地门禁完成，首版已部署到 ECS，但真实教学 provider 仍等待用户填写 API Key 后做一次付费联调**。
第 8 轮的完整本地门禁、生产 PostgreSQL 迁移、HTTPS、备份恢复、worker 常驻和回滚资产均已有证据；本地当前未运行 PostgreSQL，因此没有把生产库当测试库，也没有重复声称全量 PG 门禁刚刚重跑。部署入口见 `deploy/production/README.md`。
发布收口已完成：新 ECS `120.55.115.162` 已部署 `0013`、HTTPS、备份恢复和最终服务状态复核；注册/登录核心闭环已真跑，DeepSeek 最小真实请求已成功并记录 usage，生产平台预算采用 `NULL` 月度金额 cap（保留超时、租约和未知结果边界）。

## 2026-09-22 发布收口记录

- [x] 本机 `E:\pgsql` PostgreSQL 注册为 `study-plan-postgresql` 自动服务，`NetworkService` 运行，重启后可用。
- [x] 本机真实 PostgreSQL 全套回归：`765 passed, 1 skipped`；PG 专项无 skip；修复应用期望 schema 仍停在 `0012` 的启动缺陷。
- [x] ECS 迁移 `0012 -> 0013`，当前 release 为 `20260922-provider-parser-v3`；web、ingestion、teaching 均 active，healthz 200。
- [x] ECS `platform_budget_config` 为 `monthly_cap_micro = NULL`、`paid_dispatch_enabled = true`；仍保留请求超时、worker lease、未知结果对账和 provider 响应解析边界。
- [x] 最小真实 DeepSeek 请求成功：`run_zj_ZR1pfWRgyjYrwgHhJlg`，`inference_only`，usage `338/172`，成本记录 `682 micro`；答案已清理 provider 前置推理文本并持久化。
- [x] 生产备份恢复到临时库通过，迁移版本 `0013`、最新成功运行和平台配置均可读回，临时库已删除。
- [x] 记录四次真实 provider 尝试：前两次暴露纯文本 JSON 解析问题，第三次暴露前置推理污染，第四次成功；每次 usage/成本均进入平台结算。

## 目标

把学习规划助手的想法完善成边界清晰、可验证、可分阶段实施的产品与技术设计；覆盖学习诊断、路径规划、资料获取、知识库、RAG、练习与验收、可观测性、并发、延迟、错误处理和任务隔离。

## 当前阶段

- [x] 阶段 1：检查项目现状与现有约束
- [x] 阶段 2：确认目标用户、产品形态、规模和成功标准
- [x] 阶段 3：调研并比较主流/前沿方案（具体版本与许可证仍需在依赖锁定前实时核验）
- [x] 阶段 4：提出架构路线及推荐
- [x] 阶段 5：分节确认完整设计
- [x] 阶段 6：写入正式设计文档并自检
- [x] 阶段 7：完成最终设计整合与实施计划
- [x] 阶段 8：固化分层 RAG、工具调用校验/重试与多级证据状态策略
- [x] 阶段 9：第一轮产品化底座——PostgreSQL 持久化、邀请会话与产品 API

## 已知约束

- 前端使用 React。
- 第一阶段产品形态为多用户 SaaS。
- 一个用户可以针对不同学习内容创建多个学习项目，每个学习项目可包含多个会话。
- 首个垂直领域聚焦 Agent 工程，包括 Agent harness、编程语言、React、Redis 等相关技术栈及未来趋势。
- 学习方式以项目驱动、干中学为主，目标是交付能解决真实问题的 Agent 产品，而不是演示型 Demo。
- 后端语言在 Python 与 TypeScript 中选择或组合。
- 必须先评估用户基础，再制定高效学习路径。
- 必须支持多渠道资料获取和用户自有知识库。
- 每个阶段必须通过成果、习题或其他手段验证学习效果。
- 必须基于知识掌握度做针对性学习和必要前置知识扩展。
- 需要明确 Agent、工具、任务与程序之间的权限和隔离边界。
- 需要覆盖高并发、延迟、错误处理与可观测性。
- 平台需要在隔离沙箱中运行用户代码，用于依赖安装、测试、构建和工程验收；首版不提供长期在线的完整云开发/托管环境。
- 容量按约 10 万注册用户、500 峰值同时活跃用户设计，首发资源按约 1,000 注册用户、20 峰值同时活跃用户部署。
- 模型策略：本地开源模型只做路由和轻任务，不承担高质量生成；复杂或不确定任务优先升级云端。
- 首版优先选开源免费组件、MCP 和技能；商业托管模型的调用成本单独管理。

## 决策记录

- 2026-09-17：项目目录为空且不是 Git 仓库，按绿地项目进行设计；在用户确认设计前不实施。
- 2026-09-17：由于范围包含多个独立子系统，先形成总体架构，再拆分为可独立交付的子项目。
- 2026-09-17：核心数据层级采用“用户 → 学习项目 → 会话”；计划、资料、掌握度和长期记忆默认隔离在学习项目内。
- 2026-09-17：首版采用 Agent 工程垂直路线；以真实产品目标反推能力图谱、先修知识、学习任务和验收证据。
- 2026-09-17：代码验收选择短生命周期隔离执行沙箱；执行面与控制面、数据面分离。
- 2026-09-17：采用“成长容量设计、验证规模部署”，避免早期过度基础设施投入，同时保留横向扩展能力。
- 2026-09-17：采用模块化单体控制面、独立异步工作节点和独立沙箱执行面；首版不采用全微服务。
- 2026-09-17：正式规格拆为总体设计、系统不变量、学习闭环、路由检索、策略执行和多租户治理六份文档。
- 2026-09-17：首版默认开放 A0/A1 明确子类，A2/A3 在授权协议与确认 UI 完成后逐工具解锁。
- 2026-09-18：根据最终评审补齐 taint endorsement、独立审计/KMS/出网边界、Evidence 投影语义、公共内容、生命周期、break-glass、复测分级和拒绝路径机械门。
- 2026-09-18：完成最终整合设计与分任务实施计划；实施阶段按计划逐项执行，不在设计文档阶段直接创建业务代码。
- 2026-09-18：补充 05 号规格（Agent 技能分层、上下文管理与工具边界）与 06 号规格（分层架构、层间通信与模块契约），填补六份规格中「Agent 运行时上下文层」的空白。
- 2026-09-18：确认 ChildRun 深度上限 1（放宽需 ADR）、`docs/skills/` 独立于 `docs/superpowers/`、工具 registry 为机器真相源而 tool-catalog 为生成视图。
- 2026-09-18：L1 契约清单保留扩展余地（新增成本须为 O(1)，允许 `status: planned` 预留条目）；首批只落 `sql-schema`、`protocol`、`tool-catalog` 三份，先验证机制本身是否有效，通过后再补其余五份。
- 2026-09-18：回改 00 号（新增不变量 15–17 与两条威胁）、02 号（上下文装配与按需加载）、03 号（工具声明字段、重叠 CI 门、ChildRun 一节）、总设计（§5 增两个模块、§12/§13 更新）；新增 ADR-013/014；实施计划新增任务 9 并排入批次二。
- 2026-09-18：按「端口适配器」策略生成批次一最小闭环：核心域纯标准库，持久化/沙箱/检索/模型为开发适配器，并在 README 显式列出 11 项未实现及其后果；88 个不变量测试全部通过。
- 2026-09-18：代码推送至私有仓库 `https://github.com/tiance002/study-agent-platform`。因本机 git 传输被代理阻断（CONNECT tunnel failed），改用 Git Data API 快照式推送（`tools/push_via_api.py`）：远端为单个快照提交，本地保留完整开发历史。
- 2026-09-18：完成一轮独立代码审查的修复（4×P1 + 1×P2 + 1 项主动补充）：契约生成机制改为内容级一致性校验；API 项目边界改以**路径为权威**；审计与预算增加结构化租户/项目字段并强制过滤；预算双维度预留改为原子操作；精确回读的过滤收回 `ChunkIndex`；投影输入先按作用域过滤。测试 88 → 108。阶段判定：批次一核心不变量实现完成，发布门禁未完成。
- 2026-09-18：完成第二轮审查（2×P1 + 3×P2）的修复：新增 `app/identity` 签名会话令牌与成员归属校验，**请求体不再含任何身份字段**（刻意不做登录端点，签发为运维脚本）；A2 确认改为服务端 `ConfirmationRecord`（绑定主体/项目/工具/参数哈希/有效期，单次消费）；CI 补秘密内容扫描、ruff、mypy 共六道门禁。测试 108 → 115。
- 2026-09-18：PostgreSQL 16.4 安装至 `E:\pgsql`（免安装 binaries），应用角色 `study_app` 为 `NOSUPERUSER + NOBYPASSRLS`；**RLS 实测生效**（无租户上下文自然见 0 行、越权写入被拒）。数据库须由用户在自己终端启动。
- 2026-09-18：**确立硬约定 `scripts/*.cmd` 必须保持纯 ASCII 且行尾为 CRLF**。cmd.exe 按系统代码页逐字节解析批处理文件，UTF-8 中文会让解析偏移错位并报出乱码错误；`chcp 65001` 放在文件开头无法挽回。中文说明一律进 README，`.gitattributes` 已为 `*.cmd` 指定 `eol=crlf`。
- 2026-09-18：确认回改现有权威规格以固化分层 RAG、结构化证据问题、工具调用前校验与三套独立重试预算；不新建平行规格。`derive()` 保持通用，模型边界通过显式 `new_sources=MODEL_OUTPUT` 增加来源，不把确定性派生误标为模型输出。
- 2026-09-19：开发路线调整为“先完善底层，再交付普通用户测试版”，压缩为三轮大里程碑；第一轮只做持久化与产品底座，模型/RAG 和 React 用户界面分别留到第二、三轮。
- 2026-09-19：**冻结第一轮数据模型与接口**（`docs/superpowers/specs/2026-09-19-07-round1-product-data-model.md`）。冻结时发现执行计划与仓库现状**四处偏差**，全部解决：①`gen_sql_schema.py` 不存在，SQL 契约实为 `gen_contracts.py` 的渲染器；②幂等键改**单一出口**（`http_idempotency`，唯一键 `(tenant_id, principal_id, command_scope, client_key)`，**`project_id` 不进唯一键**）；③项目模型统一为 `identity/models.py` 的 `LearningProject`，删除 `ProjectRecord`；④`app/config.py` 只做应用级聚合，DSN 唯一来源仍是 `db/settings.py`。
- 2026-09-19：完成第一轮**任务 1**（产品契约 + `0002` 迁移）。9 张新表、`projects` 加 3 列、**策略从租户级收紧为成员感知**；SQL 契约改为**从迁移导出**（此前扫描 dataclass，与真实表结构没有任何机械联系）。实测：空库 → head 建 17 张表；`head → downgrade -1 → head` 可逆且策略真的还原；`study_app` 无上下文查 `projects` 得 **0 行**。
- 2026-09-19：完成第一轮**任务 2（身份仓储与邀请引导）+ 任务 3（签名 Cookie 会话）**。`identity/ports.py` 三协议 + 内存/PostgreSQL 双适配器跑同一套参数化契约测试；`0003` 组合外键 + `SECURITY DEFINER` 兑换函数；`POST /auth/invitations/exchange` 全程无客户端身份字段，cookie（HttpOnly/SameSite=Lax）验签 → CSRF 来源检查 → **回库查撤销**。退出标准六条全部实测达成。
- 2026-09-19：安全基线收口（审查 P1/P2 全修）+ **第 2 轮完成**（任务 4/5/6：项目 CRUD、会话/消息/计划/资料、HTTP 幂等持久化）。退出门验收：cookie 用户全流程 + 重启恢复 + 退出撤销，全部真跑 PostgreSQL。
- 2026-09-20：**第 3 轮完成**（学习闭环 MVP）：固定三问诊断、确定性计划生成、任务流转、自报提交、verified 计算结论与掌握度投影；跨聚合事务收进 `LearningLoopRepository`；493 项测试全过，PG 无 skip。
- 2026-09-20：**第 4 轮完成**（资料摄取与可核验引用）：`0007` 三张表 + FORCE RLS + append-only 授权；纯文本/Markdown 结构化切块（不改写原文，span 硬不变量由类型强制）；PG durable 队列（`FOR UPDATE SKIP LOCKED` + lease + 独立 worker 策略）；中文关键词基线检索（`keyword/v1`，确定性排序）与可核验引用（`source_id + span + content_hash` 可独立核对原文）。600 项测试全过，PG 无 skip；临时库证明 `0006 → 0007 → 0006 → 0007` 往返且每步都有 `FORCE RLS`。
- 2026-09-20：**第 4 轮收口**（独立审查 7 条，全部复核成立）：R4-04 测试库隔离（`study_test_*` 随机临时库 + 库名即凭证）；R4-01 队列的跨租户能力绑到 `study_worker` 角色（迁移 `0008`，自定义 GUC 不再当凭据）；R4-02 `claim_token` 围栏（`complete`/`fail` 比状态+token+期限，迁移 `0009`）；R4-03 引用带 `document_id` 且精确读取按标识查库，检索默认只看最新成功版本；R4-05 片段与**持久化原文**比对；R4-06 纯文本独立解析路径；R4-07 围栏比长度不只比字符。收口按风险单元 6 次提交，每条都做了注入反向验证（去掉保护 → 对应用例变红）。顺带修掉两个实现缺陷：组合根无视 `STUDY_PLATFORM_DSN`（会连业务库）与契约生成器漏读 f-string 形式的 `ADD COLUMN`。
  - 范围红线（本轮**刻意不做**，留给后续轮次）：模型教学交互、React 前端、向量检索/reranker、网页抓取、PDF/Office/OCR/embedding。检索是**可复现的关键词基线**，不是语义检索 —— 同义改写命中不了，这一点在 `findings.md` 明确记录为能力边界而非缺陷。
  - 实施偏差一处：计划假设 `PostgresKnowledgeRepository`，实际检索层无后端相关逻辑，改为**单一** `KnowledgeRepository` + 双 `IngestionRepository` 实现跑同一套隔离断言（理由回注计划 Step 2 与 `store.py` docstring）。

## 错误记录

| 错误 | 处理 |
|---|---|
| 当前目录不是 Git 仓库，无法查看提交历史 | 记录为绿地项目；设计文档阶段无法执行 Git 提交，除非用户后续初始化仓库 |
| AnySearch runtime.conf 不存在 | 按技能要求回退到运行时检测；检测到 Python 3.13 |
| 检索工具上游脚本完整性校验遭遇网络 EOF / 证书吊销检查失败 | 未执行未校验脚本；当前不声称完成实时资料核验，候选组件须在正式选型时核验 |
## 2026-09-22 · 架构精简与边界收口

- [x] 生产环境不再挂载旧开发 API；稳定健康、身份和掌握度接口保持不变。
- [x] 无 `psycopg` 的基础安装可以导入并启动内存开发模式。
- [x] 消除 `api <-> db`、`audit <-> db`、`identity <-> product` 的直接循环及 PG 对内存实现的私有调用。
- [x] 教学 provider 只从 `DeploymentSettings` 读取配置，不再二次读取进程环境。
- [x] 修正文档中的历史状态，并完成全量、PG 和静态检查。
- [x] ECS 以独立 release 发布并完成备份、启动自检、HTTPS 和服务日志冒烟。
- [x] GitHub 分支推送；提交覆盖当前生产源码与本轮架构收口，避免只上传局部修复。

实施细节：`docs/superpowers/plans/2026-09-22-architecture-cleanup.md`。
